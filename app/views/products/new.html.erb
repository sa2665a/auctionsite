<h1>Add a product</h1>

<%= form_for @products do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>

<br>

	<%= f.label :description %>
	<%= f.text_area :description %>

<br>

	<%= f.label :deadline %>
	<%= f.date_field :deadline, required: true %>
	<%= f.button %>
<% end %>	